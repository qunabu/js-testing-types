/**
 * Returns division of 2 numbers
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export const div = (a, b) => a / b;

/**
 * Returns modulo of 2 numbers
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export const mod = (a, b) => a % b;
